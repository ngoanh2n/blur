/**
 * @author Ho Huu Ngoan (ngoanh2n@gmail.com)
 * @since 2020-12-01
 */
tasks.withType(Javadoc) {
    options.addStringOption("encoding", ENCODING)
    options.addStringOption("Xdoclint:none", "-quiet")
}

modifyPom {
    project {
        name "blur"
        inceptionYear "2020"
        url "https://github.com/ngoanh2n/blur"
        description "Wrapped Selenium WebDriver"

        scm {
            url "http://github.com/ngoanh2n/blur/tree/master"
            connection "scm:git:git://github.com/ngoanh2n/blur.git"
            developerConnection "scm:git:ssh://github.com/ngoanh2n/blur.git"
        }

        licenses {
            license {
                name "MIT"
                url "https://opensource.org/licenses/MIT"
            }
        }

        developers {
            developer {
                id "ngoanh2n"
                name "Ho Huu Ngoan"
                email "ngoanh2n@gmail.com"
                organizationUrl "https://github.com/ngoanh2n"
                roles {
                    role "Owner"
                    role "Developer"
                }
            }
        }
    }
}

extraArchive {
    tests = true
    sources = true
    javadoc = true
}

nexus {
    sign = true
    repositoryUrl = "https://oss.sonatype.org/service/local/staging/deploy/maven2/"
    snapshotRepositoryUrl = "https://oss.sonatype.org/content/repositories/snapshots/"
}
